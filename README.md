# Welcome to Webber, the dev browser for WebberBeet files, an language that is 80% Markdown and 20% HTML, written in pure python. 
Pipes mean formatted text, where you can do what you need with them
in a formatted text box, you can use <> for bolding, :: for italizing, or <: :> for both.
There is also color! dirreactly after the opening |, use ** for calling a function.
We will use the builtin hexc() function. It takes in a hex code, which is defined by a H! starter.
And that is version 0.10, simple
